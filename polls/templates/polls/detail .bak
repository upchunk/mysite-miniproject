<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Polling Page</title>
  </head>
  <body>
    <section>
        <h1>{{ question.question_text }}</h1> <! - Previosly {{question}} - >
        <ul>
        {% for choice in question.choice_set.all %}
            <li>{{ choice.choice_text }}</li>
        {% endfor %}
        </ul>
    </section>
  </body>
</html>

